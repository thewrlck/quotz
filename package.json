{
  "name": "quotz",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "api": "npm start -w @quotz/api"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "tsup": "^8.2.4",
    "typescript": "^5.5.4"
  }
}
